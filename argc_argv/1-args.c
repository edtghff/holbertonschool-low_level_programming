#include <stdio.h>

/**
 * main - prints the number of arguments
 * @argc: argument
 * @argv: array
 * Return: 0
 **/

int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
